[project]
name = "stress-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ayon-python-api>=1.2.11",
]

[tool.pyright]
venv = ".venv"
venvPath = "."
